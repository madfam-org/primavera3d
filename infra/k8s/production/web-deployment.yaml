apiVersion: apps/v1
kind: Deployment
metadata:
  name: primavera3d-web
  namespace: primavera3d
  labels:
    app: primavera3d-web
    app.kubernetes.io/name: primavera3d-web
    app.kubernetes.io/part-of: primavera3d
spec:
  replicas: 2
  selector:
    matchLabels:
      app: primavera3d-web
  template:
    metadata:
      labels:
        app: primavera3d-web
        app.kubernetes.io/name: primavera3d-web
        app.kubernetes.io/part-of: primavera3d
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      imagePullSecrets:
      - name: ghcr-credentials
      containers:
      - name: primavera3d-web
        image: ghcr.io/madfam-org/primavera3d/web:latest
        ports:
        - containerPort: 3000
          name: http
        envFrom:
        - secretRef:
            name: primavera3d-secrets
            optional: true
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: [ALL]
        startupProbe:
          httpGet:
            path: /
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 30
        readinessProbe:
          httpGet:
            path: /
            port: 3000
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 3000
          periodSeconds: 30
